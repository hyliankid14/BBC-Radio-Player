<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
    Google Play Build Flavor Manifest
    
    This manifest is merged with the main manifest for Google Play builds.
    It includes Google-specific metadata for optimized Android Auto integration,
    allowing the system to auto-discover the app as an Android Auto compatible app.
    
    This variant includes com.google.android.gms.car.application metadata
    which is specific to Google Play Store distribution and not included in
    the F-Droid build flavor (see app/src/fdroid/AndroidManifest.xml).
    -->
    <application>
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
    </application>
</manifest>
