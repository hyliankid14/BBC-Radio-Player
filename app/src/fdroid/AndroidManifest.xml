<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
    F-Droid Build Flavor Manifest
    
    This manifest is merged with the main manifest for F-Droid builds.
    It does NOT include Google-specific metadata like the GMS car application
    declaration, keeping the app clean and free of proprietary dependencies
    as required by F-Droid's inclusion policy.
    
    Android Auto functionality still works through the standard
    MediaBrowserService interface defined in the main manifest.
    -->
</manifest>
